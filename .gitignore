.venv/
__pycache__/
*.pyc
*.pyo
.DS_Store
.ipynb_checkpoints/

# Everything in data/ is regeneratable: CSV caches via scripts/fetch_data.py,
# multigas parquets via scripts/convert_tyler_extracts.py, per-block resource
# parquet rebuilt on first plot_main.py run.
data/

# Local credentials (Dune / ClickHouse). Never commit.
.env

# Local-only planning notes — keep on disk, off the remote.
docs/simulation_plan.md
docs/demand_elasticity_plan.md

"""
Wallet-level clustering — placeholder.

Companion to `tx_clustering.py`, but groups WALLETS (tx_sender addresses)
instead of individual transactions. The interesting axes here will likely be:
    • aggregated resource mix per wallet (CLR over Σ_tx g_{tx,k})
    • activity volume / frequency / inter-arrival times
    • dominant counterparties (contracts called)

Empty for now — drop the implementation in once tx_clustering.py is settled.
"""
